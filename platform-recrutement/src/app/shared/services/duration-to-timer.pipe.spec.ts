import { DurationToTimerPipe } from './duration-to-timer.pipe';

describe('DurationToTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationToTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
